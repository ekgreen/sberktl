spring:
  jpa:
    database: h2
    open-in-view: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS AUTH
    name: db
    username: sa
    password: sa